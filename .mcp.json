{
  "mcpServers": {
    "loan-data": {
      "command": "python",
      "args": ["-m", "loan_mcp.server"],
      "env": {
        "API_BASE_URL": "http://localhost:8000",
        "PYTHONPATH": "src"
      }
    }
  }
}
